################################################################################
# APPLICATION CONFIGURATION
################################################################################

APP_NAME="POS Faro"
APP_ENV=local
APP_DEBUG=true
APP_KEY=
APP_URL=http://localhost

APP_LOCALE=es_MX
APP_FALLBACK_LOCALE=en
APP_TIMEZONE=America/Mexico_City

################################################################################
# DATABASE CONFIGURATION
################################################################################
# Local Development (SQLite)
DB_CONNECTION=sqlite
# DB_DATABASE=database/database.sqlite

# Production (MySQL 8.0+)
# Uncomment below and configure for production environments
# DB_CONNECTION=mysql
# DB_HOST=127.0.0.1
# DB_PORT=3306
# DB_DATABASE=pos_faro
# DB_USERNAME=pos_user
# DB_PASSWORD=your_secure_password_here

################################################################################
# CACHE CONFIGURATION
################################################################################
# Supported: file, database, redis, memcached, dynamodb, null
# Database (default for development)
CACHE_STORE=database
CACHE_PREFIX=pos_

# Redis (recommended for production)
# CACHE_STORE=redis
# REDIS_HOST=127.0.0.1
# REDIS_PASSWORD=null
# REDIS_PORT=6379
# REDIS_CACHE_DB=1

################################################################################
# QUEUE CONFIGURATION
################################################################################
# Supported: sync, database, redis, sqs, null
# Database (default for development - runs jobs immediately)
QUEUE_CONNECTION=database

# Redis (recommended for production - async job processing)
# QUEUE_CONNECTION=redis
# REDIS_QUEUE_HOST=127.0.0.1
# REDIS_QUEUE_PASSWORD=null
# REDIS_QUEUE_PORT=6379
# REDIS_QUEUE_DB=0

################################################################################
# SESSION CONFIGURATION
################################################################################
# Supported: cookie, database, memcached, redis
SESSION_DRIVER=database
SESSION_LIFETIME=120
SESSION_ENCRYPT=false

################################################################################
# FILE STORAGE CONFIGURATION
################################################################################
# Local storage (default for development)
FILESYSTEM_DISK=public

# S3 Storage (recommended for production)
# FILESYSTEM_DISK=s3
# AWS_ACCESS_KEY_ID=your_aws_access_key
# AWS_SECRET_ACCESS_KEY=your_aws_secret_key
# AWS_DEFAULT_REGION=us-east-1
# AWS_BUCKET=pos-faro-bucket
# AWS_URL=https://bucket.s3.amazonaws.com
# AWS_VISIBILITY=public
# AWS_ENDPOINT=null

MEDIA_DISK=local

################################################################################
# MAIL/EMAIL CONFIGURATION
################################################################################
# Development (Log all emails to storage/logs)
MAIL_MAILER=log

# Production - SMTP Configuration (Gmail, SendGrid, etc.)
# MAIL_MAILER=smtp
# MAIL_HOST=smtp.gmail.com
# MAIL_PORT=587
# MAIL_USERNAME=your-email@gmail.com
# MAIL_PASSWORD=your-app-password
# MAIL_ENCRYPTION=tls
# MAIL_FROM_ADDRESS=noreply@pos-faro.com
# MAIL_FROM_NAME="${APP_NAME}"

# Production - SendGrid
# MAIL_MAILER=sendgrid
# SENDGRID_API_KEY=your-sendgrid-api-key

# Production - Mailgun
# MAIL_MAILER=mailgun
# MAILGUN_DOMAIN=mg.pos-faro.com
# MAILGUN_SECRET=key-xxx-xxx

# Production - AWS SES
# MAIL_MAILER=ses
# AWS_SES_REGION=us-east-1

MAIL_FROM_ADDRESS="noreply@pos-faro.com"
MAIL_FROM_NAME="${APP_NAME}"

################################################################################
# SMS CONFIGURATION
################################################################################
# Development (Log SMS to storage/logs)
SMS_FROM="+5215512345678"
SMS_DRIVER=log

# Production - Twilio
# SMS_DRIVER=twilio
# TWILIO_ACCOUNT_SID=your_account_sid
# TWILIO_AUTH_TOKEN=your_auth_token
# TWILIO_FROM_SMS=+1234567890
# TWILIO_STATUS_WEBHOOK_URL=https://pos-faro.com/webhooks/sms/status

# Production - Vonage (Nexmo)
# SMS_DRIVER=vonage
# VONAGE_API_KEY=your_api_key
# VONAGE_API_SECRET=your_api_secret
# VONAGE_FROM_SMS=POS Faro

################################################################################
# CARONTE OIDC & BEE-HIVE MULTITENANCY
################################################################################
# Register the callback exactly as written in the Caronte client. HTTPS and
# certificate verification are mandatory outside local/test environments.
CARONTE_URL=https://caronte.example.com
CARONTE_APP_CN=pos
CARONTE_APP_SECRET=
CARONTE_APPLICATION_GROUP_ID=
CARONTE_APPLICATION_GROUP_SECRET=
CARONTE_ISSUER_ID=caronte
CARONTE_TOKEN_CLOCK_SKEW_SECONDS=60
CARONTE_OIDC_ISSUER=https://caronte.example.com
CARONTE_OIDC_CLIENT_ID=pos
CARONTE_OIDC_CLIENT_SECRET=
CARONTE_OIDC_REDIRECT_URI=https://pos.example.com/auth/oidc/callback
CARONTE_OIDC_JWKS_CACHE_TTL=3600
CARONTE_ALLOW_HTTP_REQUESTS=false
CARONTE_TLS_VERIFY=true

BEE_HIVE_TENANT_KEY=tenant_id
BEE_HIVE_RESOLVER=Ometra\Caronte\Tenancy\Resolvers\CaronteTenantResolver
# Local/test only. Never set a static tenant in production.
BEE_HIVE_STATIC_TENANT_ID=

################################################################################
# LOGGING & MONITORING
################################################################################
LOG_CHANNEL=stack
LOG_STACK=daily
LOG_LEVEL=debug

# Log Sensitive Data
LOG_SENSITIVE=false

# Structured Logging (JSON format)
LOG_FORMAT=single

# Ray (Optional Remote Debugging)
RAY_ENABLED=false
RAY_HOST=127.0.0.1

# Sentry (Optional Error Tracking - Production)
# SENTRY_LARAVEL_DSN=your_sentry_dsn
# SENTRY_ENVIRONMENT=${APP_ENV}
# SENTRY_TRACES_SAMPLE_RATE=0.1
# SENTRY_RELEASE=1.0.0

################################################################################
# VITE & FRONTEND CONFIGURATION
################################################################################
VITE_APP_NAME="${APP_NAME}"
VITE_API_BASE_URL=http://localhost
VITE_ENABLE_DEVTOOLS=true

################################################################################
# MAINTENANCE MODE
################################################################################
APP_MAINTENANCE_DRIVER=file
APP_MAINTENANCE_STORE=database

################################################################################
# LOCALIZATION & FAKER
################################################################################
APP_FAKER_LOCALE=es_MX

# Previous encryption keys (for rotating APP_KEY)
APP_PREVIOUS_KEYS=

################################################################################
# RATE LIMITING & SECURITY
################################################################################
# Rate Limiting
RATE_LIMIT_ENABLED=true
RATE_LIMIT_REQUESTS_PER_MINUTE=60
RATE_LIMIT_AUTH_ATTEMPTS=5

# CORS Configuration
CORS_ALLOWED_ORIGINS=http://localhost,http://localhost:3000
CORS_ALLOWED_METHODS=GET,POST,PUT,DELETE,PATCH,OPTIONS
CORS_ALLOWED_HEADERS=Content-Type,Authorization,X-Requested-With
CORS_EXPOSE_HEADERS=Authorization
CORS_CREDENTIALS=true
CORS_MAX_AGE=3600

################################################################################
# SANCTUM API TOKEN CONFIGURATION
################################################################################
# Stateful domains that can receive stateful API authentication cookies
SANCTUM_STATEFUL_DOMAINS=localhost,localhost:3000,127.0.0.1,127.0.0.1:8000,::1

# Token expiration in minutes (null = never expires)
# SANCTUM_EXPIRATION=null

################################################################################
# FEATURE FLAGS
################################################################################
# Audit Logging
FEATURE_AUDIT_LOGGING=true

# Advanced Reporting
FEATURE_ADVANCED_REPORTS=true

# Inventory Management
FEATURE_INVENTORY_TRACKING=true

# Multi-warehouse Support
FEATURE_MULTI_WAREHOUSE=true

################################################################################
# THIRD-PARTY INTEGRATIONS (Optional)
################################################################################

# AWS Services
# AWS_ACCESS_KEY_ID=
# AWS_SECRET_ACCESS_KEY=
# AWS_DEFAULT_REGION=us-east-1

# Slack Notifications
# SLACK_BOT_USER_OAUTH_TOKEN=
# SLACK_BOT_USER_DEFAULT_CHANNEL=#pos-alerts
# SLACK_WEBHOOK_URL=

# Google Cloud Services
# GOOGLE_APPLICATION_CREDENTIALS=/path/to/credentials.json
# GOOGLE_CLOUD_PROJECT_ID=
# GOOGLE_CLOUD_STORAGE_BUCKET=

# Analytics (Optional)
# GOOGLE_ANALYTICS_ID=
# MIXPANEL_TOKEN=

################################################################################
# POS PROVIDER INTEGRATIONS
################################################################################
# Default mock drivers allow local development, SQLite tests, and CI without
# external payment, fiscal, printer, cash drawer, or barcode hardware.
POS_PAYMENT_DRIVER=mock
POS_PAYMENT_MOCK_FAIL=false

POS_FISCAL_DRIVER=mock
POS_FISCAL_ISSUE_ON_CHECKOUT=false
POS_FISCAL_MOCK_FAIL=false

POS_RECEIPT_PRINTER_DRIVER=mock
POS_RECEIPT_PRINT_ON_CHECKOUT=true
POS_RECEIPT_PRINTER_MOCK_FAIL=false

POS_CASH_DRAWER_DRIVER=mock
POS_CASH_DRAWER_OPEN_ON_CASH_CHECKOUT=true
POS_CASH_DRAWER_MOCK_FAIL=false

POS_BARCODE_SCANNER_DRIVER=mock
POS_ERP_DRIVER=stub
POS_OUTBOX_MAX_ATTEMPTS=8

################################################################################
# BIRD FLOCK (Queue Management)
################################################################################
# Only needed if using Bird Flock package
BIRD_FLOCK_DEFAULT_QUEUE=default
BIRD_FLOCK_TABLE_PREFIX=bird_flock_
BIRD_FLOCK_LOGGING_ENABLED=true
BIRD_FLOCK_DLQ_ENABLED=true
BIRD_FLOCK_MAX_PAYLOAD_SIZE=262144
BIRD_FLOCK_BATCH_INSERT_CHUNK_SIZE=500
BIRD_FLOCK_HEALTH_ENABLED=true

# Circuit Breaker Pattern
BIRD_FLOCK_CIRCUIT_BREAKER_FAILURE_THRESHOLD=5
BIRD_FLOCK_CIRCUIT_BREAKER_TIMEOUT=60
BIRD_FLOCK_CIRCUIT_BREAKER_SUCCESS_THRESHOLD=2

# Retry Configuration
BIRD_FLOCK_SMS_MAX_ATTEMPTS=3
BIRD_FLOCK_SMS_BASE_DELAY_MS=1000
BIRD_FLOCK_SMS_MAX_DELAY_MS=60000

################################################################################
# DEVELOPMENT & DEBUG TOOLS
################################################################################

# Telescope (Laravel Debug Tool)
# TELESCOPE_ENABLED=true
# TELESCOPE_STORAGE=database

# Debugbar (Query & Profiling)
# DEBUGBAR_ENABLED=true

# PsySH REPL
# PSYSH_ENABLED=true

################################################################################
# PRODUCTION CHECKLIST
################################################################################
# Before deploying to production, ensure:
# 1. APP_ENV=production
# 2. APP_DEBUG=false
# 3. APP_KEY is set (run: php artisan key:generate)
# 4. Database is configured (MySQL 8.0+)
# 5. Redis is configured for Cache/Queue
# 6. Mail driver is configured (SMTP/SendGrid/etc)
# 7. SMS driver is configured (Twilio/Vonage/etc)
# 8. File storage is configured (S3/local with backups)
# 9. Sentry/Error tracking is enabled
# 10. SSL/TLS is enabled (HTTPS)
# 11. All sensitive values are in .env (not in .env.example)
# 12. Backups are configured and tested
# 13. Monitoring and alerts are set up
# 14. Logging is configured for JSON output
# 15. Rate limiting and CORS are properly configured
# 16. Caronte issuer, client, callback and application credentials are set
# 17. BEE_HIVE_STATIC_TENANT_ID is empty
# 18. No enabled POS integration uses mock or stub drivers
# 19. /up and /ready return HTTP 200 before receiving traffic
